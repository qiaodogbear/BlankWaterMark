# Examples

- `sample-payload.json` contains a small JSON payload suitable for DCT mode on medium or large images.
- `sample-image.png` is generated by `npm run examples`.

Use a larger image or lower repetition if the UI reports insufficient capacity.
